<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Authors Table</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container my-5">
    <h2 class="text-center text-secondary fw-bold mb-4">Our Featured Authors</h2>
    
    <div class="row">
      <div class="col-md-12">
        {% if authors is empty %}
          <div>Not Found</div>
        {% else %}
          <table class="table">
            <tbody>
            {% for author in authors %}
              <tr>
                <td>
                  <div class="card border-0 bg-light shadow-sm">
                    <div class="row g-0 align-items-center">
                      <div class="col-md-2 text-center p-3">
                        <img src="{{ author.picture }}" alt="Author Picture" class="rounded-circle img-fluid" style="width: 60px; height: 60px;">
                      </div>
                      <div class="col-md-8">
                        <div class="card-body">
                          <h5 class="card-title">{{ author.username }}</h5>
                          <p class="card-text"><strong>Email: </strong>{{ author.email }}</p>
                          <p class="card-text"><strong>Books Published: </strong>{{ author.nb_books }}</p>
                        </div>
                      </div>
                      <div class="col-md-2 text-center p-3">
                        <a href="/author/{{author.id}}" class="btn btn-primary btn-sm rounded-pill">View Profile</a>
                      </div>
                    </div>
                  </div>
                </td>
              </tr>
            {% endfor %}
            </tbody>
          </table>
          
          <!-- Affichage du nombre total des livres et auteurs -->
          <div class="mt-4">
            <p><strong>Total Authors:</strong> {{authors |length}}</p>
            {% set total_books = 0 %}
            {% for author in authors %}
            {% set total_books = total_books + author.nb_books %}
            {% endfor %}
            <p><strong>Total Books Published:</strong> {{ total_books }}</p>
          </div>
        {% endif %}
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
